# Local build/module caches
.cache/

# Build outputs
bin/
dist/
.tmp/

# Vendor (generated via `make vendor`, not committed)
vendor/

# Local-only planning artifact; never commit
PLAN.md

# Local agent tooling state (pi todos, installed skills, skill lockfile)
.agents/
.pi/
skills-lock.json

# OS/editor noise
.DS_Store
*.swp
*.swo
